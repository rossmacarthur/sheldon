version = "{{ version }}"
home = "{{ root }}"
root = "{{ root }}"
config_file = "{{ config_file }}"
lock_file = "{{ lock_file }}"

[[plugins]]
name = "test"
directory = "{directory}"
filenames = ["{filename}"]
apply = ["source"]

[templates]
